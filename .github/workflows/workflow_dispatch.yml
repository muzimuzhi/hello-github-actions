name: workflow_dispatch

on:
  workflow_dispatch:

jobs:
  job:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Hello from workflow_dispatch placeholder"
          echo "\`github.event_name=${{ github.event_name }}\`" >> "$GITHUB_STEP_SUMMARY"
