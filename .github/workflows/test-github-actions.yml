on: [push]

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      MY_VAR: GitHub
    steps:
      - run: mkdir -p temp
      - run: echo "hello world"
      - run: echo "hello $MY_VAR"
        working-directory: temp
